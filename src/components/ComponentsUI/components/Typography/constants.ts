// @home-book
import { DARK_COLORS, LIGHT_COLORS } from 'home-book-constants';
import { Theme } from 'home-book-types';

export const DEFAULT_COLOR_MODE = {
  [Theme.dark]: DARK_COLORS.neutral1,
  [Theme.light]: LIGHT_COLORS.neutral1,
};
